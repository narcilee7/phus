# Phus Issues

1. boostrap的填写API Key的时候，paste不生效
2. phus的config yaml的watch不太对，应该是路径有问题(迁移monorepo)后，是不是可以phus根目录写进去而不是写到某一个包？
3. CLI就是TUI，TUI就是CLI，而不是一个TUI command OR命令.
4. 下一步要做的是治理这个项目，重构成更多小的monorepo运行时，统一一条app cli(之后可能有别的app，web/gui/mobile)
5. 现在还没有release，测试还需要跑到tui下面pnpm run。。。很蠢

