packages:
  - "packages/shared"
  - "packages/core"
  - "packages/runtime"
  - "packages/tui"
  - "apps/cli"

# TypeScript 7 is intentional; tsdown/rolldown-plugin-dts still declare a
# ^5 peer, so we relax peer-dependency enforcement workspace-wide.
strictPeerDependencies: false
