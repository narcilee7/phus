{
	"name": "@phus/core",
	"version": "0.1.0",
	"private": true,
	"description": "Phus core: hooks, tape, skill registry, policy, plugin loader, types. Headless — no CLI, no I/O outside the SQLite tape. Stage 1 migrates these out of @phus/runtime.",
	"license": "ISC",
	"type": "module",
	"main": "./src/index.ts",
	"types": "./src/index.ts",
	"exports": {
		".": "./src/index.ts",
		"./types/*": "./src/types/*",
		"./utils/*": "./src/utils/*"
	},
	"files": [
		"dist",
		"README.md"
	],
	"scripts": {
		"build": "echo 'no-op in Stage 0 — code lands here in Stage 1'",
		"build:js": "echo 'no-op in Stage 0'",
		"build:types": "echo 'no-op in Stage 0'",
		"typecheck": "tsc --noEmit",
		"lint": "oxlint --quiet src test",
		"test": "echo 'no tests yet — Stage 0 stub'"
	},
	"engines": {
		"node": ">=20.0.0"
	}
}
