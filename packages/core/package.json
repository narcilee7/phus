{
	"name": "@phus/core",
	"version": "0.1.0",
	"private": true,
	"description": "Phus core: headless public surface — types, constants, hook/tape/skill interfaces. Re-exports from @phus/runtime today; physical files will move here in follow-up commits. See documents/Proposal-Monorepo-Split.md §3.",
	"license": "ISC",
	"type": "module",
	"main": "./dist/index.js",
	"types": "./dist/index.d.ts",
	"exports": {
		".": {
			"types": "./dist/index.d.ts",
			"import": "./dist/index.js",
			"default": "./dist/index.js"
		}
	},
	"files": [
		"dist",
		"README.md"
	],
	"scripts": {
		"build": "tsc --outDir dist && tsc-alias -p tsconfig.json && node ../../scripts/fix-extensions.mjs dist",
		"build:js": "tsc --outDir dist && tsc-alias -p tsconfig.json && node ../../scripts/fix-extensions.mjs dist",
		"build:types": "tsc --emitDeclarationOnly --declaration --outDir dist",
		"typecheck": "tsc --noEmit",
		"lint": "oxlint --quiet src test",
		"test": "vitest run --passWithNoTests"
	},
	"engines": {
		"node": ">=20.0.0"
	},
	"peerDependencies": {
		"@phus/runtime": "workspace:*"
	},
	"devDependencies": {
		"tsc-alias": "^1.9.1"
	}
}
