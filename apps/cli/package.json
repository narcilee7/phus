{
	"name": "@phus/cli",
	"version": "0.1.0",
	"private": true,
	"description": "The Phus CLI bin: `phus` (no args) wakes the TUI; every subcommand runs here. Stage 2 will move the composition root out of @phus/runtime.",
	"license": "ISC",
	"type": "module",
	"main": "./src/index.ts",
	"types": "./src/index.ts",
	"exports": {
		".": "./src/index.ts"
	},
	"files": [
		"dist",
		"README.md"
	],
	"scripts": {
		"build": "echo 'no-op in Stage 0 — bin lands here in Stage 2'",
		"build:js": "echo 'no-op in Stage 0'",
		"build:types": "echo 'no-op in Stage 0'",
		"dev": "tsx src/index.ts",
		"typecheck": "tsc --noEmit",
		"lint": "oxlint --quiet src test",
		"test": "echo 'no tests yet — Stage 0 stub'"
	},
	"engines": {
		"node": ">=20.0.0"
	}
}
